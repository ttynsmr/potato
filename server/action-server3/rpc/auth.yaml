# yaml -> torikime -> generate .proto & rpc code.
contracts:
  auth:
    login:
      request:
        user_id: string
        password: string
      response:
        success: bool
        ok: bool
        token: string