# yaml -> torikime -> generate .proto & rpc code.
contracts:
  auth:
    server_ready:
      notification:
        ok: bool
    login:
      request:
        user_id: string
        password: string
      response:
        ok: bool
        token: string
