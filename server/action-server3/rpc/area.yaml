# yaml -> torikime -> generate .proto & rpc code.
contracts:
  area:
    transport:
      notification:
        area_id: uint32
        unit_id: uint64
    setup_dynamic_trigger:
      imports:
        - "area"
      notification:
        area_id: uint32
        triggers:
          container: array
          type: potato.Trigger
