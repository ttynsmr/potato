# yaml -> torikime -> generate .proto & rpc code.
contracts:
  example:
    update_mouse_position:
      imports:
        - "message.proto"
      request:
        position: potato.Vector3
      response:
        ok: bool
      notification:
        session_id: int32
        position: potato.Vector3
    spawn:
      imports:
        - "message.proto"
      notification:
        session_id: int32
        position: potato.Vector3
    despawn:
      notification:
        session_id: int32
